# board.ts テスト仕様書

対象: `src/utils/board.ts`
関連仕様: `docs/spec.md` 4.1章・12章

## createInitialBoard

| #   | ケース                                  | 前提・入力                                    | 期待される結果                                             |
| --- | --------------------------------------- | --------------------------------------------- | ---------------------------------------------------------- |
| 1   | 戻り値は9x9の二次元配列                 | 9x9の `given`（一部が数値、一部がnull）       | 戻り値の長さが9、各行の長さが9                             |
| 2   | givenが数値のセルはヒントとして扱われる | `given[r][c]` が数値                          | 対応するセルの `isGiven === true`、`value === given[r][c]` |
| 3   | givenがnullのセルは未入力として扱われる | `given[r][c] === null`                        | 対応するセルの `isGiven === false`、`value === null`       |
| 4   | 全セルのmemosは初期状態ですべてnone     | 任意のgiven（ヒント・空欄どちらのセルも含む） | 全セルの `memos[1]`〜`memos[9]` がすべて `'none'`          |

## hasProgress（spec.md 5.6章「進行中の盤面」判定）

| #   | ケース                                          | 前提・入力                                             | 期待される結果 |
| --- | ------------------------------------------------ | ---------------------------------------------------------- | ---------------- |
| 5   | ユーザー入力が1マスもなければfalse              | ヒントのみで構成された盤面（未入力マスは全てvalue=null） | `false`          |
| 6   | ユーザー入力が1マスでもあればtrue                | 未入力マスのうち1つに `value` を設定した盤面              | `true`           |
| 7   | ヒントマス（isGiven=true）の値は進行中と見なさない | ヒント以外は全て未入力の盤面                              | `false`          |

## countPlacedValues（Issue #21: 入力済み数字ボタンの無効化判定）

| #   | ケース                                             | 前提・入力                                                    | 期待される結果                                          |
| --- | --------------------------------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------- |
| 8   | 戻り値は1〜9それぞれの配置数を持つ                 | 全マス未入力の盤面（ヒントなし）                                | 戻り値の `1`〜`9` の各値が `0`                              |
| 9   | ヒントマスの値もカウントに含まれる                 | ある数字がヒントとして3マスに配置された盤面                     | その数字の戻り値が `3`                                      |
| 10  | ユーザー入力（value）もカウントに含まれる          | ある数字をユーザーが5マスに入力した盤面                         | その数字の戻り値が `5`                                      |
| 11  | ヒントとユーザー入力を合算してカウントする         | ある数字がヒント2マス・ユーザー入力7マスで計9マスに配置された盤面 | その数字の戻り値が `9`                                      |
| 12  | 正誤を問わず配置数のみをカウントする（誤答も含む） | 正解でない数字を含めて9マス配置された盤面                       | その数字の戻り値が `9`                                      |
