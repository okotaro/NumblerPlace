# Controls.tsx テスト仕様書

対象: `src/components/Controls.tsx`
関連仕様: `docs/spec.md` 2章・5.2章〜5.6章、`prompts/01_全体図.jpg`

## 表示

| #   | ケース                                                                              | 前提・入力 | 期待される結果                                                                                                                   |
| --- | ----------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 戻る・消しゴム・メモON/OFF・Check・New Gameの各ボタンと数字パッド(1〜9)が表示される | なし       | それぞれの名前を持つボタンが存在する（戻る・消しゴム・メモON/OFFはアイコンボタンで、`aria-label`によりアクセシブルネームを持つ） |

## 操作

| #   | ケース                                                      | 前提・入力               | 期待される結果                             |
| --- | ----------------------------------------------------------- | ------------------------ | ------------------------------------------ |
| 2   | 戻るボタンをクリックするとonUndoが呼ばれる                  | 戻るボタンをクリック     | `onUndo` が1回呼ばれる                     |
| 3   | 消しゴムボタンをクリックするとonEraseが呼ばれる             | 消しゴムボタンをクリック | `onErase` が1回呼ばれる                    |
| 4   | メモON/OFFボタンをクリックするとonToggleMemoModeが呼ばれる  | メモボタンをクリック     | `onToggleMemoMode` が1回呼ばれる           |
| 5   | 数字ボタンをクリックするとonNumberClickがその数字で呼ばれる | "5"のボタンをクリック    | `onNumberClick` が `5` を引数に1回呼ばれる |
| 6   | Checkボタンをクリックするとon Checkが呼ばれる               | Checkボタンをクリック    | `onCheck` が1回呼ばれる                    |
| 7   | New Gameボタンをクリックするとon NewGameが呼ばれる          | New Gameボタンをクリック | `onNewGame` が1回呼ばれる                  |

## メモON/OFFの状態表示（spec.md 5.4章）

| #   | ケース                                          | 前提・入力          | 期待される結果                           |
| --- | ----------------------------------------------- | ------------------- | ---------------------------------------- |
| 8   | isMemoMode=trueのときメモボタンがON状態を示す   | `isMemoMode: true`  | メモボタンの `aria-pressed` が `"true"`  |
| 9   | isMemoMode=falseのときメモボタンがOFF状態を示す | `isMemoMode: false` | メモボタンの `aria-pressed` が `"false"` |

## completedNumbersの受け渡し（Issue #21）

| #   | ケース                                                  | 前提・入力                | 期待される結果                                       |
| --- | ---------------------------------------------------------- | ---------------------------- | ------------------------------------------------------- |
| 10  | completedNumbersがNumberPadにそのまま渡され無効化される | `completedNumbers={[5]}`     | "5"のボタンが `disabled` になる                          |
