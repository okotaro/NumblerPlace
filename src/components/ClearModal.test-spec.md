# ClearModal.tsx テスト仕様書

対象: `src/components/ClearModal.tsx`
関連仕様: `docs/spec.md` 9章

## 表示

| #   | ケース                                             | 前提・入力      | 期待される結果                                       |
| --- | ---------------------------------------------------- | ---------------- | -------------------------------------------------------- |
| 1   | isOpen=falseのとき何も表示されない                  | `isOpen: false`  | ダイアログ（`role="dialog"`）が存在しない               |
| 2   | isOpen=trueのときクリアした旨のメッセージが表示される | `isOpen: true`  | ダイアログが表示され、クリアを伝えるメッセージが含まれる |
| 3   | isOpen=trueのときNew Gameへの導線が表示される        | `isOpen: true`  | "New Game" ボタンが表示される                            |

## 操作

| #   | ケース                                              | 前提・入力                     | 期待される結果            |
| --- | ------------------------------------------------------ | --------------------------------- | ---------------------------- |
| 4   | New Gameボタンをクリックするとon NewGameが呼ばれる     | New Gameボタンをクリック          | `onNewGame` が1回呼ばれる    |
