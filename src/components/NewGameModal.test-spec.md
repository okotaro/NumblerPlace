# NewGameModal.tsx テスト仕様書

対象: `src/components/NewGameModal.tsx`
関連仕様: `docs/spec.md` 5.6章・7章

## 表示

| #   | ケース                                                      | 前提・入力                          | 期待される結果                                                       |
| --- | ------------------------------------------------------------- | -------------------------------------- | ---------------------------------------------------------------------- |
| 1   | isOpen=falseのとき何も表示されない                          | `isOpen: false`                        | ダイアログ（`role="dialog"`）が存在しない                              |
| 2   | isOpen=trueのとき5つの難易度選択肢が表示される              | `isOpen: true`                         | `Easy`/`Medium`/`Hard`/`Expert`/`Master` の5つのラジオボタンが表示される |
| 3   | 現在のdifficultyに対応する選択肢が選択済みになっている      | `isOpen: true`, `difficulty: 'hard'`   | `Hard` のラジオボタンが `checked`                                       |
| 4   | 「決定」ボタンと「キャンセル」ボタンが表示される            | `isOpen: true`                         | "決定" ボタンと "キャンセル" ボタンが表示される                        |

## 操作

| #   | ケース                                                      | 前提・操作                          | 期待される結果                                        |
| --- | ------------------------------------------------------------- | -------------------------------------- | -------------------------------------------------------- |
| 5   | 難易度の選択肢をクリックするとonSelectDifficultyが呼ばれる  | `Expert` のラジオボタンをクリック      | `onSelectDifficulty` が `'expert'` を引数に1回呼ばれる    |
| 6   | 「決定」ボタンをクリックするとonConfirmが呼ばれる           | 「決定」ボタンをクリック               | `onConfirm` が1回呼ばれる                                 |
| 7   | 「キャンセル」ボタンをクリックするとonCancelが呼ばれる      | 「キャンセル」ボタンをクリック         | `onCancel` が1回呼ばれる                                  |
