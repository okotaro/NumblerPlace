# Cell.tsx テスト仕様書

対象: `src/components/Cell.tsx`
関連仕様: `docs/spec.md` 3章・4章・4.1章・13章、`prompts/01_盤面マス.jpg`

## 表示

| #   | ケース                                                            | 前提・入力                                                             | 期待される結果                                                            |
| --- | ----------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| 1   | 未入力マスは何も表示しない                                        | `value: null`, `isGiven: false`, 全memoが`'none'`                      | 数字・メモのテキストが表示されない                                        |
| 2   | 解答入力済みマスは数字を中央に大きく表示する                      | `value: 6`, `isGiven: false`                                           | "6" が表示される                                                          |
| 3   | 確定マス（ヒント）は数字を表示し、ヒント用スタイルが付く          | `value: 6`, `isGiven: true`                                            | "6" が表示され、ヒント用のスタイル（例: `data-given="true"`）が付与される |
| 4   | 解答済みマスはメモを表示しない（value優先）                       | `value: 6`, `isGiven: false`, `memos: {1: 'candidate'}`                | メモのミニグリッド内の数字（"1"など）は表示されない。"6" のみ表示される   |
| 5   | 候補メモは3x3ミニグリッドの対応位置に通常表示される               | `value: null`, `memos: {1: 'candidate', 9: 'candidate'}`, 他は`'none'` | ミニグリッドの左上に "1"、右下に "9" が取り消し線なしで表示される         |
| 6   | 非候補メモは3x3ミニグリッドの対応位置に取り消し線付きで表示される | `value: null`, `memos: {5: 'notCandidate'}`, 他は`'none'`              | ミニグリッド中央に "5" が取り消し線付きで表示される                       |
| 14  | 解答値の表示は視認性向上のため拡大・太字スタイルを持つ（Issue #8） | `value: 6`, `isGiven: false`                                           | 数字を表示する`<span>`が`text-2xl`と`font-bold`のクラスを持つ             |
| 17  | メモ数字は視認性向上のため、通常時より大きいサイズ・太めのウェイトで表示される（候補・非候補共通、Issue #12） | `value: null`, `memos: {1: 'candidate'}`, 他は`'none'`                 | メモを表示するコンテナ要素が`text-sm`と`font-medium`のクラスを持つ        |
| 18  | 非候補メモの取り消し線は、数字のストロークと重なって視認性が下がらないよう強調されたスタイルを持つ（Issue #12） | `value: null`, `memos: {5: 'notCandidate'}`, 他は`'none'`              | `<s>`要素が取り消し線を強調するクラス（`decoration-2`, `decoration-red-500`）を持つ |

## 選択・関連ハイライト

| #   | ケース                                                           | 前提・入力                              | 期待される結果                                                  |
| --- | ---------------------------------------------------------------- | --------------------------------------- | --------------------------------------------------------------- |
| 7   | 選択中マスには選択スタイルが付く                                 | `isSelected: true`                      | `data-selected="true"` が付与される                             |
| 8   | 関連マス（選択マスと同じ行・列・ブロック）には関連スタイルが付く | `isRelated: true`                       | `data-related="true"` が付与される（`data-selected`は付かない） |
| 9   | 無関係のマスにはどちらのスタイルも付かない                       | `isSelected: false`, `isRelated: false` | `data-selected`・`data-related` がいずれも付かない              |
| 15  | 選択マスと同じ数字を持つマスには同値スタイルが付く（Issue #3）   | `isSameValue: true`                     | `data-same-value="true"` が付与される                            |
| 16  | 同じ数字を持たないマスには同値スタイルが付かない（Issue #3）     | `isSameValue: false`                    | `data-same-value` が付与されない                                 |

## エラー表示（spec.md 8章・13章）

| #   | ケース                                 | 前提・入力      | 期待される結果                       |
| --- | --------------------------------------- | --------------- | ------------------------------------- |
| 12  | isError=trueのときエラースタイルが付く | `isError: true` | `data-error="true"` が付与される      |
| 13  | isError=falseのときエラースタイルは付かない | `isError: false` | `data-error` が付与されない       |

## ヒント表示（spec.md 13章・15章、Issue #22・#26）

`hintRole?: 'cause' | 'eliminated'` を受け取り、`cause`（技法の根拠マス）と`eliminated`
（候補が除去されるマス）で異なる強調スタイルを付与する。

| #   | ケース                                       | 前提・入力      | 期待される結果                        |
| --- | ----------------------------------------------- | --------------- | -------------------------------------- |
| 19  | hintRole="cause"のときヒント用の強調スタイルが付く | `hintRole: 'cause'`  | `data-hint="true"`・`data-hint-role="cause"` が付与される |
| 19b | hintRole="eliminated"のとき候補除去用の強調スタイルが付く | `hintRole: 'eliminated'`  | `data-hint="true"`・`data-hint-role="eliminated"` が付与される |
| 20  | hintRoleが未指定のときヒント用の強調スタイルは付かない | `hintRole: undefined` | `data-hint` が付与されない         |

## 操作

| #   | ケース                                                                 | 前提・入力      | 期待される結果           |
| --- | ---------------------------------------------------------------------- | --------------- | ------------------------ |
| 10  | マスをクリックするとonSelectが呼ばれる                                 | 通常のセル      | `onSelect` が1回呼ばれる |
| 11  | 確定マス（ヒント）をクリックしてもonSelectは呼ばれる（選択自体は可能） | `isGiven: true` | `onSelect` が1回呼ばれる |
