# numberPlaceService.ts テスト仕様書

対象: `src/services/numberPlaceService.ts`
関連仕様: `docs/spec.md` 7章・12章

## generatePuzzle

| #   | ケース                                 | 前提・入力                                                                                                                                                            | 期待される結果                                            |
| --- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| 1   | 難易度ごとに9x9の盤面が返る            | difficulty = `'easy'` \| `'medium'`（`hard`/`expert`/`master` はsudoku-coreの生成時間が数十秒に及ぶことがあるためフル検証の対象外とし、Difficulty型で受理を保証する） | given・solutionともに9x9の二次元配列                      |
| 2   | 生成される正解盤面が数独として妥当     | 上記と同じ                                                                                                                                                            | 各行・列・3x3ブロックに1〜9が重複なく含まれる             |
| 3   | ヒントセルは正解と一致する             | difficulty = `'medium'`                                                                                                                                               | `given[r][c]` がnullでない場合、`solution[r][c]` と同じ値 |
| 4   | 生成された盤面はヒントと空欄が混在する | difficulty = `'medium'`                                                                                                                                               | givenに非null値とnullの両方が存在する                     |

## DEFAULT_DIFFICULTY

| #   | ケース                            | 期待される結果                    |
| --- | --------------------------------- | --------------------------------- |
| 1   | 既定難易度はmedium（spec.md 7章） | `DEFAULT_DIFFICULTY === 'medium'` |

## DIFFICULTIES

| #   | ケース                                                          | 期待される結果                                                       |
| --- | ---------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 1   | 5段階の難易度が選択UIの表示順で公開される（spec.md 7章・12章） | `DIFFICULTIES` が `['easy', 'medium', 'hard', 'expert', 'master']` と一致 |

## isBoardComplete

| #   | ケース         | 前提・入力              | 期待される結果 |
| --- | -------------- | ----------------------- | -------------- |
| 1   | 全マス入力済み | 81マスすべて非null      | `true`         |
| 2   | 1マスでも空    | 1マスがnull、他は非null | `false`        |
| 3   | 全マス空       | 81マスすべてnull        | `false`        |

## checkAnswers

| #   | ケース                                  | 前提・入力                      | 期待される結果                    |
| --- | --------------------------------------- | ------------------------------- | --------------------------------- |
| 1   | 全マス正解と一致                        | userValues = solutionと同一     | 全マス `false`                    |
| 2   | 1マスだけ不一致                         | 1マスをsolutionと異なる値に変更 | そのマスのみ `true`、他は `false` |
| 3   | 未入力マスは誤りとしない（spec.md 8章） | 1マスをnullに変更               | そのマスは `false`                |

## findHint（spec.md 15章、Issue #22）

`src/services/hint/hintFinder.ts` の `findHint` をそのまま委譲する薄いラッパー。詳細な技法判定ロジックのテストは `hintFinder.test.ts` 側で担保するため、ここでは委譲が正しく行われることのみ確認する。

| #   | ケース                                   | 前提・入力                           | 期待される結果                                                    |
| --- | ------------------------------------------ | --------------------------------------- | ---------------------------------------------------------------------- |
| 1   | Naked Singleが検出できる盤面でHintが返る | 完成盤面から1マスだけ空欄にした盤面     | 空欄マスの位置・値を持ち `technique: 'nakedSingle'` のHintが返る       |
| 2   | 技法が該当しない盤面ではnullが返る       | 全マス入力済みの盤面                    | `null` が返る                                                          |
